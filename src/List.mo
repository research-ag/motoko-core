/// Original: `Buffer.mo`

import { todo } "Debug";

module {
  public class List<T>(initCapacity : Nat) {

    public func contains(key : T) : Bool = todo();

    public func get(index : Nat) : T = todo();

    // TODO...
  }
}
